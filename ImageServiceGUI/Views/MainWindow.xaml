<Window x:Class="ImageServiceGUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ImageServiceGUI="clr-namespace:ImageServiceGUI;assembly=ImageServiceGUI"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:ImageServiceGUI.Views"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        Title="MVVM"
        Height="300"
        Width="600">
    <Grid x:Name="LayoutRoot"
          Background="{Binding VM_Connected}">

        <TabControl>
            <TabItem Header="Settings">
                <views:SettingsView/>
            </TabItem>
            <TabItem Header="Logs">
                <views:LogsView/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>